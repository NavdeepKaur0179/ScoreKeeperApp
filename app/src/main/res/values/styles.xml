<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>
    <style name="ScoreButtons" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_background</item>

    </style>
    <style name="MinusButton" parent="ScoreButtons">
        <item name="android:src">@drawable/img_button_minus_src</item>
        <item name="contentDescription">@string/minus_button</item>
        <item name="android:layout_marginStart">@dimen/margin_button</item>
    </style>
    <style name="PlusButton" parent="ScoreButtons">
        <item name="android:src">@drawable/img_button_plus_src</item>
        <item name="contentDescription">@string/plus_button</item>
        <item name="android:layout_marginEnd">@dimen/margin_button</item>

    </style>

    <style name="ScoreText">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Display3</item>
    </style>
    <style name="TeamText">
        <item name=
            "android:textAppearance">@style/TextAppearance.AppCompat.Display1
        </item>
    </style>

</resources>
